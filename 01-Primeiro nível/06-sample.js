const nU = "Pedro";
const iA = 25;
const iU = true;

function dU(nU, iA, iU) {
    if (iU) {
        console.log(`${nU  } tem ${  iA  } anos e está ativo.`);
    } else {
        console.log(`${nU  } está inativo.`);
    }
}

dU(nU, iA, iU);
